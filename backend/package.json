{
  "name": "backend",
  "version": "1.0.0",
  "description": "",
  "main": "index.js",
  "scripts": {
    "start": "node server.js",
    "dev": "nodemon server.js",
    "start:pm2": "pm2 start server.js --name feelify-backend",
    "logs:pm2": "pm2 logs feelify-backend",
    "stop:pm2": "pm2 stop feelify-backend && pm2 delete feelify-backend || true",
    "generate:prisma": "prisma generate",
    "format:prisma": "prisma format",
    "postinstall": "prisma generate",
    "test": "echo \"Error: no test specified\" && exit 1"
  },
  "keywords": [],
  "author": "",
  "type": "module",
  "license": "ISC",
  "dependencies": {
    "@prisma/client": "^6.18.0",
    "cors": "^2.8.5",
    "dotenv": "^17.2.3",
    "express": "^5.1.0",
    "node-fetch": "^3.3.2"
  },
  "devDependencies": {
    "nodemon": "^3.1.7",
    "prisma": "^6.18.0"
  }
}
